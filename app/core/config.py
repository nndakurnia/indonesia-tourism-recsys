from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent.parent.parent

DATA_PATH = BASE_DIR / "data" / "tourism_with_id.csv"
# MODEL_PATH = BASE_DIR / "data" / "model.json"
# METADATA_PATH = BASE_DIR / "data" / "metadata.json"
# PLACES_PATH = BASE_DIR / "data" / "places.json"
API_VERSION = "1.0.0"
